<%= simple_form_for [:account,@answer] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content %>
    <!%= f.input :is_hidden %>
    <!%= f.input :user_id %>
    <!%= f.input :question_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
