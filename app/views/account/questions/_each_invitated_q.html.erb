<%# @invitated_questions.each do |question| %>
<li class="list-group-item">
  <div class="media">
    <div class="pull-left">
      <div class="panel-heading">
        <h3 class="panel-title">问题主题：<%= invitated_question.title %> 提问人：<%= invitated_question.user.name %></h3>
      </div>

      <div class="panel-body">
        <p>问题描述：<%= sanitize(invitated_question.description) %></p>
      </div>

      <!-- <% answers = current_user.answers.where(question_id:invitated_question.id) %>
      <% answers.each do |answer| %>
        <div class="panel panel-info ">
          <div class="panel-heading">
            <h3 class="panel-title">我的回答</ h3>
          </div>
            <div class="panel-body invitated_users_answer">
              <%= answer.content %>

              <div class="total clearfix">
                <span class="pull-right">
                  <%= link_to("编辑答案", edit_account_answer_path(answer), class:"btn btn-danger btn-xs") %>
                  <%#= render :partial => "account/questions/show_button_base_status", :locals => { :question => question, :answer => answer } %>
                </span>
              </div>
            </div>
       </div>
      <% end %> -->


      <div class="total clearfix btn-right-bottom">
        <span class="pull-right">
          <% if invitated_question.status=='open' %>
            <%= link_to '回答' ,new_question_answer_path(invitated_question),class:"btn btn-xs btn-danger" %>
          <% else %>
            <%= link_to('问题已关闭', 'javascript:void(0)',class:"btn btn-xs btn-default",disabled:true)%>
          <% end %>
        </span>
      </div>

    <%# end %>

    </div>
  </div>
</li>
