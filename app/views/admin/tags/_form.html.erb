<%= simple_form_for([:admin,@tag]) do |f| %>
  <div class="form-inputs">
    <%= f.input :name,label:"标签名称" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit,value:"提交问题" %>
  </div>
<% end %>
